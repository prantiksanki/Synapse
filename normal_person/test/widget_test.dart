import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder test', (WidgetTester tester) async {
    // Placeholder — real tests require a running signaling server.
    expect(true, isTrue);
  });
}
